// todo: node tests

var SETTINGS = require("./../settings");

module.exports = function (grunt) {

  grunt.registerTask('test:server', 'run our server tests', function () {
    console.log("-------------------------------------------------");
    console.log("server tests!");
    console.log("-------------------------------------------------");
  });

};